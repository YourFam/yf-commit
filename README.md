# yf-commit
CLI that writes a conventional commit from your staged git diff: emoji type(scope), WHY, WHAT CHANGED, files. Init once — provider, model (Auto = lightweight), your API key. OpenAI-compatible (OpenAI, DeepSeek, xAI, Groq, Ollama). Does not run tests or append machine/worktree trailers.
